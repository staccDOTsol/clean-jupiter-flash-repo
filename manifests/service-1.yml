apiVersion: v1
kind: Service
metadata:
    name: "test-d214"
    labels:
        app: "test-d214"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "test-d214"