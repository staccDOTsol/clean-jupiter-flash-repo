apiVersion: v1
kind: Service
metadata:
  name: {{ include "charts.yaml.fullname" . }}
  labels:
    {{- include "charts.yaml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "charts.yaml.selectorLabels" . | nindent 4 }}
